#! /bin/sh
#
#
TMPDIR=${TMPDIR:-$TMP}
TMPDIR=${TMPDIR:-$TEMP}
export TMPDIR
TEMP=`mktemp -d -t texthint.XXXXXX` || exit 1

LVMPATH=${LVMPATH:-".:/usr/lib/helium/lib"}
export TEMP LVMPATH

/usr/lib/helium/bin/texthint $@
ERR=$?

rm -rf $TEMP
exit $ERR

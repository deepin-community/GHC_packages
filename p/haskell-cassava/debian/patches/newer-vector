--- a/cassava.cabal
+++ b/cassava.cabal
@@ -70,7 +70,7 @@ Library
     hashable < 1.3,
     text < 1.3,
     unordered-containers < 0.3,
-    vector < 0.12
+    vector < 0.13
 
   -- GHC.Generics lived in `ghc-prim` for GHC 7.2 & GHC 7.4 only
   if impl(ghc < 7.6)

#!/usr/bin/make -f

PREFIX = debian/bustle/usr

%:
	dh $@

override_dh_auto_build:
	mkdir -p dist/build
	dh_auto_build

override_dh_auto_install:
	$(MAKE) install PREFIX=$(PREFIX)
	cabal install --prefix=$(PREFIX)

#! /bin/sh
# prerm script for hmake

set -e

case "$1" in
    remove|upgrade|deconfigure|failed-upgrade)
        haskell-utils --remove-triggers hmake
    ;;
    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 0
    ;;
esac

#DEBHELPER#

exit 0

--- a/haxr.cabal
+++ b/haxr.cabal
@@ -35,8 +35,7 @@
 Library
   Build-depends: base < 5,
                  base-compat >= 0.8 && < 0.10,
-                 mtl,
-                 mtl-compat,
+                 mtl >= 2.2.1,
                  network < 2.7,
                  http-streams,
                  HsOpenSSL,

--- a/cabal-install.cabal
+++ b/cabal-install.cabal
@@ -236,7 +236,7 @@
   build-depends:
     Cabal,
     HUnit,
-    QuickCheck >= 2.1.0.1 && < 2.8,
+    QuickCheck >= 2.1.0.1,
     base,
     bytestring,
     directory,

#!/bin/sh

set -e

GHC_PKG=/usr/bin/ghc-pkg6

case "$1" in
configure|abort-upgrade|abort-remove|abort-deconfigure)
	${GHC_PKG} update /@PKGLIB_DIR@/@PKG@.package.conf ;;
*)
	echo "postinst called with unknown argument \`$1'" >&2
	exit 1 ;;
esac

#DEBHELPER#

exit 0

#!/bin/sh -e

case "$1" in
configure|abort-upgrade|abort-remove|abort-deconfigure)
	ghc-pkg register /@PKGLIB_DIR@/@PKG@.package.conf ;;
*)
	echo "postinst called with unknown argument \`$1'" >&2
	exit 1 ;;
esac

#DEBHELPER#

exit 0

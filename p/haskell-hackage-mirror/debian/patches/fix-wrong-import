--- a/main/Main.hs
+++ b/main/Main.hs
@@ -23,10 +23,10 @@ import Options.Applicative
       info,
       help,
       header,
-      fullDesc,
-      (<>) )
+      fullDesc )
 import Control.Monad.Logger
        (filterLogger, LogLevel(LevelDebug), runStdoutLoggingT)
+import Data.Monoid ((<>))
 
 main :: IO ()
 main =

--- a/snaplet-acid-state.cabal
+++ b/snaplet-acid-state.cabal
@@ -25,7 +25,7 @@
     Snap.Snaplet.AcidState

   build-depends:
-    acid-state                >= 0.6     && < 0.11,
+    acid-state                >= 0.6     && < 0.12,
     base                      >= 4       && < 5,
     snap                      >= 0.6     && < 0.13,
     text                      >= 0.11    && < 0.12

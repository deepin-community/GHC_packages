--- a/hledger-web.cabal
+++ b/hledger-web.cabal
@@ -129,8 +129,8 @@ library
     if flag(dev) || flag(library-only)
         cpp-options: -DDEVELOPMENT
     build-depends:
-          hledger-lib == 0.27
-        , hledger == 0.27
+          hledger-lib == 0.27.1
+        , hledger == 0.27.1
         , base               >= 4 && < 5
         , base-compat        >= 0.8.1
         , blaze-html
